export interface EquipmentReefer {
  is_controlled_atmosphere?: string; // Si es atmosfera controlada
  is_starvent?: string; // Si es un starvent
  is_super_freeze?: string; // Si es un super freeze
  is_temperature_controlled?: string; // Si es controlado por temperatura
  rfr_type?: string; // Tipo de reefer
}
