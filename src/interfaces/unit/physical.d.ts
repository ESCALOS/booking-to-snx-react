export interface Physical {
  has_wheels?: string; // Si tiene ruedas
  height_mm?: string; // Altura
  is_insulated?: string; // Si está insulado
  is_permanently_out_of_service?: string; // Si está fuera de servicio
  iso_group?: string; // Grupo de iso
  length_mm?: string; // Longitud
  material?: string; // Material
  strength_code?: string; // Código de resistencia
  tare_weight_kg?: string; // Peso tare
  width_mm?: string; // Ancho
  grade?: string; // Grado
}
