export interface Event {
  id?: string; // ID del evento
  time_event_applied?: string; // Fecha y hora del evento aplicado
  user_id?: string; // ID del usuario
  is_billable?: string; // Es facturable
}
